# Production Environment Configuration
# Copy this file to .env and update with your production values

# Server Configuration
PORT=3000
NODE_ENV=production

# MongoDB Connection (Use MongoDB Atlas)
# Get your URI from MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/magister_portal_db

# JWT Secret (Generate a new secure random string!)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-production-jwt-secret-here-change-this

# Email Configuration (for password recovery)
# For Gmail, use an app password: https://myaccount.google.com/apppasswords
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
ADMIN_EMAIL=admin@yourdomain.com

# Frontend URL (Your actual domain)
FRONTEND_URL=https://www.magisterportal.com

# Allowed Origins (comma-separated - your domain(s))
ALLOWED_ORIGINS=https://www.magisterportal.com,https://magisterportal.com

# Optional: Error monitoring
SENTRY_DSN=
